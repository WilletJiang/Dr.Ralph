# Early Validations

Create one folder per validation item, for example:

- `ri-004/`
- `ri-005/`

Each folder should contain:
- commands run
- configs used
- raw logs
- result summary
- interpretation
